using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Interact_Parent : MonoBehaviour
{

    //interaction reference tag
    private string interact_Tag;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //returns tag
    public string get_Int_Tag()
    {
        return interact_Tag;
    }
}
